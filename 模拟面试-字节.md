# 模拟面试 — 字节跳动前端（一面）

> 面试官风格：字节一面通常 45-60 分钟，前半段深挖项目，后半段前端基础 + 手撕代码。追问很深，喜欢问"为什么"和"怎么做取舍"。

---

## 第一部分：自我介绍 + 项目深挖（约 25 分钟）

**面试官：同学你先简单自我介绍一下吧。**

我目前在北京化工大学读计算机技术硕士，预计 2027 年 7 月毕业。本科在大连工业大学学的网络工程。实习在博华信智做了半年前端，负责一个工业设备监测平台的前端开发，技术栈是 Vue.js + ECharts。个人项目是一个全栈 AI 聊天平台叫 Sky Chat，基于 Next.js + TypeScript + PostgreSQL，支持流式对话、联网搜索、图片生成，还有一套自研的 SSE 性能监控系统。整个项目从设计到部署都是我一个人做的。

---

**面试官：好，那你先说说 Sky Chat 这个项目，SSE 流式传输这块你是怎么做的？**

**S**：AI 聊天的核心体验是流式输出——用户发一条消息，服务端持续推送 AI 的回复。我需要选择一种服务端推送方案。

**T**：在 SSE 和 WebSocket 之间做选型。AI 聊天是"客户端发一次请求，服务端持续回复"的单向推送，不需要双向通信。

**A**：我选了 SSE。三个原因——场景上单向足够、部署上走 HTTP 兼容 Serverless、实现上 Next.js API Route 原生支持 ReadableStream。但原生 EventSource 只能用 GET，带不了消息历史。所以我用 Fetch 发 POST、通过 response.body.getReader() 手动消费流。代价是需要自己处理粘包，我写了一个 SSEParser 类按 \n\n 切事件。另外我设计了一套类型化 Chunk 协议，15 种类型，覆盖文本、推理、工具调用全生命周期。服务端翻译上游 API 的原始响应，客户端只消费标准化的 Chunk——换 API 供应商只改服务端。

**R**：SSE 在 Serverless 环境稳定运行，客户端跟服务端的 Chunk 协议保证了前后端解耦。

---

**面试官：好，你提到类型化 Chunk 协议，15 种类型分别是什么？怎么分发的？**

三类。文本类 3 种——text-start/delta/end，携带 id 和 delta。推理类 3 种——reasoning-start/delta/end，结构和文本类一样但渲染到不同的 UI 区域。工具调用类 5 种——tool-input-start 开始调工具，tool-input-delta 接收参数增量，tool-input-available 参数收完可以执行，tool-output-available 拿到结果，tool-output-error 执行失败。剩下 4 种是生命周期事件——step-start、finish、error、abort。

分发靠 switch-case。客户端收到 SSE event，JSON.parse 后校验 type 字段，按类型走不同的回调函数。

**追问：为什么工具调用要分 start、delta、available 三个阶段？不能一个阶段吗？**

因为工具调用的参数也是流式到达的，不是一次给全。start 让 UI 立刻弹出工具卡片，delta 让用户看到参数逐步填充，available 才代表参数接收完毕可以执行。如果只用一个类型，UI 就得等参数全到了才知道有工具调用，那中间几百毫秒的空白用户不知道发生了什么。

---

**面试官：你说用有限状态机管理消息生命周期，具体有几个状态？怎么转换的？**

五个状态：idle、thinking、tool_calling、answering、error。

转换逻辑：用户发消息 → idle 变 thinking。收到第一个 text-delta → thinking 变 answering（真正开始输出文字了）。中间收到 tool-input-start → 变 tool_calling（调工具）。工具执行完 → 回 answering（继续输出）。收到 finish → 回 idle。出错 → error，用户点停止 → idle。

状态存在 Zustand store 里，所有 setStatus 调用都集中在 SSE chunk 回调那一处。UI 层只读 status 决定渲染什么——组件不知道状态是怎么变的，也不需要知道。

**追问：为什么不用 if-else？直接用布尔变量判断不好吗？**

三个布尔变量（isThinking、isAnswering、isToolCalling）有 8 种组合，但只有 4 种是合法的。if-else 没办法从类型层面禁止非法组合——代码里某处忘了关掉 isAnswering 就把 isToolCalling 设成 true，两个同时为 true 了。状态机用单一变量赋值天然保证互斥。另外 this 项目 Function Calling 导致状态非常频繁地来回切，if-else 散落在各处的判断很快不可维护。

---

**面试官：你简历上说渲染频次从 120 次降到 40 次以下，怎么做到的？**

**S**：LLM API 每秒推送 30-120 个 SSE chunk，每个 chunk 触发一次 Zustand setState → React re-render。为什么不加 buffer 就是 120 次？因为每个 chunk 在 await reader.read() 之后到达，每次都在新的微任务里。React 的自动批处理只能合并同一微任务内的 setState，跨微任务合并不了。

**A**：写了一个 StreamBuffer 类。push 只往数组追加，不触发渲染。同一帧内只注册一次 requestAnimationFrame。rAF 回调里 splice 取出所有 delta、join 拼接、一次性写入 store。选 rAF 是因为它跟屏幕 60fps 刷新率同步，天然限频。流结束时 forceFlush 不等下一帧。

**R**：实测从峰值 120 次降到平均 32 次/秒。

**追问：120 和 32 怎么测出来的？**

Zustand setter 里埋计数器，performance.now() 计时每秒打印。也用了 React DevTools Profiler 交叉验证。用 performance.now 而不用 Date.now 是因为它是单调时钟，不受 NTP 校时影响。

**追问：有没有遇到过计时不准的情况？**

四种。系统休眠时 performance.now 在 Windows 上继续走，可能误报几万毫秒的 Stall，需要上限过滤。TTFB 含 TCP 建连，拆不开网络和模型推理各自耗时。rAF buffer 让 tracker 记时比用户实际看到早 0-16ms。主线程忙时 reader.read 回调被推迟，记的是回调执行时间不是数据到达时间。这些不是 bug，是设计指标时需要明确"你测的到底是什么"。

---

**面试官：你再讲一下 StreamBuffer 的工作流程，push 之后发生了什么？**

push 把 delta 推到 queue 数组里。然后检查 rafId，如果是 null 说明当前帧还没注册过 rAF，就调 requestAnimationFrame 注册一个。下一帧 rAF 回调执行时，splice 取出 queue 里所有 delta、join 拼成一个字符串、通过 flushCallback 一次性写入 Zustand。同时 rafId 置 null。如果 flush 期间 queue 又有了新数据，递归调度下一个 rAF。

同一帧里 push 一百次，只在第一次注册一个 rAF，后面九十九次只往数组追加。所以 setState 的频率从"每个 chunk 一次"变成了"每帧一次"。

**追问：为什么同一个 buffer 没有给 reasoning-delta 用？**

两个原因。reasoning 的到达频率远低于 text——只有少数模型的 thinking 阶段才出现，实际频次远不到 60fps，合并收益很小。第二个原因更关键——text-delta 和 reasoning-delta 是交替到达的，如果共用 buffer，两类 delta 混在同一个 queue 里，flush 时分不清该追加到 text part 还是 reasoning part。要正确做需要两个独立 buffer，维护成本翻倍但收益很小。

---

**面试官：你说做了 SSE 性能监控，TTFB、TTLB、Stall 怎么算的？怎么采的？**

我写了一个 SSEPerformanceTracker 类。start() 记开始时间、onChunk() 每个 chunk 到达时调——第一个 chunk 记首字节时间算 TTFB、同时跟前一个 chunk 的时间间隔算 Stall（超过 500ms 判定为一次卡顿）、finish() 算 TTLB。

500ms 阈值是基于 LLM 正常 chunk 间隔 10-50ms 倒推的，500ms 意味着约 10-50 个 token 的延迟，用户已经明显感知。

采到的数据走完整链路——先写 IndexedDB 持久化，每 10 秒批量取 20 条通过 sendBeacon 上报，成功后从 IndexedDB 删除。sendBeacon 失败降级到 Fetch + keepalive。管理后台用 ECharts 展示趋势图、卡顿分布柱状图和散点图，再加 P50/P90/P99 统计和健康评分。

**追问：sendBeacon 任务管理器杀进程能发出去吗？**

不能。sendBeacon 只跟 JS 线程解耦，没跟浏览器进程解耦。进程被杀了什么都没了。所以 IndexedDB 才是真正的最后防线——数据先持久化到磁盘，进程被杀后重新打开浏览器，数据还在，下次定时器会自动重试。

---

**面试官：JWT 为什么放 HttpOnly Cookie？**

防止 XSS 窃取。HttpOnly 标记让 JS 完全读不到这个 Cookie——document.cookie 不会返回它。如果放 localStorage，任何注入的恶意脚本都能偷走 token 发送到攻击者服务器。HttpOnly 模式下攻击者即使 XSS 成功也只能"用"（浏览器自动携带），不能"偷"。另外加了 secure（生产环境 HTTPS）和 sameSite:'lax'（防 CSRF）。

**追问：sameSite 三个值有什么区别？**

Strict——任何跨站请求都不带 Cookie，最安全但也最严格，从外部链接点进来是未登录状态。Lax——跨站 GET 和导航带 Cookie，跨站 POST 和 iframe 不带，是平衡安全性和体验的默认值。None——跨站全带，但必须配合 secure=true（强制 HTTPS）。

---

## 第二部分：前端基础（约 15 分钟）

**面试官：说一下 React 的 Fiber 架构和之前有什么区别？**

React 15 是递归的 Stack Reconciler，递归过程不可中断。一旦开始渲染就得等整棵树递归完，主线程被长时间占用，用户输入没响应——掉帧。

React 16 引入 Fiber，把渲染拆成一个个可中断的单元。每个 Fiber 节点对应一个组件或 DOM 元素，用链表（child/sibling/return）连接。渲染时实现了时间切片——每执行一个 Fiber 单元检查一次是否超时（默认 5ms），超时就暂停，把主线程还给浏览器处理用户交互，等下一帧继续。

另外 Fiber 让任务有了优先级——用户输入比动画比数据更新更优先执行。

**追问：Fiber 的链表结构是什么样的？**

每个 Fiber 节点有三个指针。child 指向第一个子节点，sibling 指向下一个兄弟节点，return 指向父节点。遍历是深度优先——先 child，没有 sibling 时回 return。这种结构的好处是中断后可以精确恢复——知道当前处理到哪个节点、它的父节点是谁，不需要重新来。

**追问：双缓冲（Double Buffering）是什么？**

Fiber 架构里有两棵 Fiber 树——current 树是屏幕上显示的，workInProgress 树是正在构建的。每次更新在 workInProgress 树上做 diff，完成后把两棵树对调——指针一交换就完成了更新。这样做的好处是渲染过程中屏幕一直显示 current 树，用户看不到不完整的中间状态。和图形学里"前缓冲区"和"后缓冲区"的逻辑一样。

---

**面试官：React 18 的 Concurrent 模式，useTransition 和 useDeferredValue 有什么区别？**

useTransition 是开发者主动标记"这个更新优先级低"。调用 startTransition 包裹的 setState 会被标记为低优先级，React 在它执行过程中如果遇到高优先级更新（用户输入），会中断它先处理高优先级的。同时 isPending 可以让 UI 展示一个过渡状态。

useDeferredValue 是 React 自动帮你降级一个值。不需要你手动标记哪个 setState 重要，只需要说"这个值可以延迟更新"。React 会在高优先级更新完成后自动更新它。

一个主动一个被动。useTransition 适合"用户点了一个按钮触发耗时操作"，useDeferredValue 适合"输入框里打字、联想列表可以慢一点更新"。

---

**面试官：React 的 useEffect 和 useLayoutEffect 有什么区别？什么场景用 useLayoutEffect？**

执行时机不同。useEffect 在浏览器绘制到屏幕之后异步执行，不阻塞渲染。useLayoutEffect 在 DOM 更新完成之后、浏览器绘制之前同步执行，会阻塞渲染。

useLayoutEffect 的场景：需要在浏览器绘制前同步读取 DOM 尺寸并做调整。比如你要根据弹窗内容高度计算弹窗位置，不能用 useEffect——用户会先看到弹窗在错误位置闪一帧、再跳到正确位置。用 useLayoutEffect 在绘制前算好位置，用户就直接看到正确的。

这个项目里 MessageList 的首次滚动用的是 useLayoutEffect——消息加载完在浏览器画出来之前就把滚动条滚到底部，用户看不到滚动过程。

---

**面试官：说一下跨域，CORS 的工作原理。**

浏览器的同源策略限制了一个源的 JS 访问另一个源的资源。CORS 是服务端告诉浏览器"我允许你跨域"的机制。

简单请求——浏览器自动在请求头加 Origin，服务端返回 Access-Control-Allow-Origin，浏览器检查 Origin 是否命中，命中了就放行 JS，没命中就把响应藏起来。

复杂请求——比如 POST + application/json 这种——浏览器先发一个 OPTIONS 预检请求，问服务端：我能发 POST 吗？能带 Content-Type: application/json 吗？服务端在响应头里回答——Access-Control-Allow-Methods: POST，Access-Control-Allow-Headers: Content-Type。预检通过后浏览器才发正式请求。预检结果会被浏览器缓存一段时间（Access-Control-Max-Age）。

这个项目开发环境前后端不同端口就是跨域，需要在 API Route 里配 CORS 头。

---

**面试官：浏览器的垃圾回收机制，V8 怎么做的？**

V8 用的是分代回收。把内存分成新生代和老生代。新生代放存活时间短的对象，用 Scavenge 算法——一半空间闲置，存活对象复制到另一边，死掉的直接丢弃。这种空间换时间的策略适合"大多数对象朝生夕死"的特点。熬过两次 Scavenge 的对象晋升到老生代。

老生代用标记-清除和标记-整理。标记阶段从根对象（全局对象、调用栈）出发，遍历所有能访问到的对象打标记。没有被标记的就是垃圾，清除阶段回收。但清除后内存碎片化，再用标记-整理——把存活对象往一端移动，腾出连续空间。

增量标记是 V8 的优化——标记不用一次性做完，可以跟 JS 执行交替进行，减少停顿时间。

**追问：闭包会导致内存泄漏吗？什么情况？**

会。闭包让外层函数的变量一直被内层函数引用，垃圾回收器认为它还在用，就不回收。如果闭包引用了不必要的变量——比如闭包只用了 a，但外层作用域里还有 b，如果 V8 没有做变量裁剪优化，b 也会被一起保留。还有忘记清除定时器和事件监听——setInterval 里的闭包拿着 DOM 引用，即使 DOM 被移除了，引用还在，整个 DOM 节点就没法被回收。这个项目里 StreamBuffer 的 destroy 方法就是专门清理 rAF 和 queue 的，防止组件卸载后 rAF 回调访问已销毁的 store。

---

## 第三部分：手撕代码（约 15 分钟）

**面试官：好，我们来做一道算法题。实现一个函数，把传入的数组分成指定大小的多个块。**

```javascript
// 输入
chunk([1, 2, 3, 4, 5, 6, 7, 8], 3)

// 输出
[[1, 2, 3], [4, 5, 6], [7, 8]]
```

```javascript
function chunk(arr, size) {
  const result = [];
  for (let i = 0; i < arr.length; i += size) {
    result.push(arr.slice(i, i + size));
  }
  return result;
}
```

**追问：如果不用 slice，怎么手动实现？**

```javascript
function chunk(arr, size) {
  const result = [];
  let current = [];
  for (let i = 0; i < arr.length; i++) {
    current.push(arr[i]);
    if (current.length === size || i === arr.length - 1) {
      result.push(current);
      current = [];
    }
  }
  return result;
}
```

---

**面试官：再写一道，实现一个带并发限制的异步调度器。最多同时运行 N 个任务，一个完成就启动下一个。**

```javascript
// 用法
const scheduler = new Scheduler(2);
scheduler.add(() => request('/api/1'));  // 立即执行
scheduler.add(() => request('/api/2'));  // 立即执行
scheduler.add(() => request('/api/3'));  // 排队等待
scheduler.add(() => request('/api/4'));  // 排队等待
```

```javascript
class Scheduler {
  constructor(limit) {
    this.limit = limit;
    this.running = 0;
    this.queue = [];
  }

  add(task) {
    return new Promise((resolve, reject) => {
      const run = () => {
        this.running++;
        task()
          .then(resolve, reject)
          .finally(() => {
            this.running--;
            if (this.queue.length > 0) {
              this.queue.shift()();
            }
          });
      };

      if (this.running < this.limit) {
        run();
      } else {
        this.queue.push(run);
      }
    });
  }
}
```

**追问：能说说这个调度器和 Promise.all 的区别吗？**

Promise.all 是所有任务并发执行，没有数量限制。100 个任务同时发出 100 个请求，可能打爆服务端或者浏览器连接池。Scheduler 限制同时只跑 N 个，类似 TCP 的滑动窗口——完成一个推出一个。这个项目的监控上报 flushQueue 就是这个思路，每次最多发 20 条。

---

**面试官：好，我这边问题差不多了，你有什么想问我的吗？**

想问两个问题。第一个，团队目前的业务主要是做什么方向的？第二个，团队对新人有什么期望——前三个月希望我做到什么程度？

---

> **面试要点回顾**
>
> 1. 每个回答都有 **S-T-A-R** 结构，背景一句话，方案是主体
> 2. 回答控制在 1-2 分钟内，不展开讲细节除非被追问
> 3. 追问环节展现了"经得起深挖"——每个追问都有具体回答，不回避"你做的有什么问题"
> 4. 手撕代码时先写简单版本再优化，边写边讲思路
> 5. 反问环节不要空着，表现对岗位的关注
