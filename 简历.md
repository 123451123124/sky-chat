# 姜家旺 - 前端工程师

::: left

icon:info 男/2001.10

icon:weixin jwww0910

:::

::: right

icon:phone 13390066330

icon:email <573799893@qq.com>

:::

## 教育背景

:::left
**北京化工大学 - 计算机技术**
:::
:::right
**2024.09 - 2027.07**
:::

:::left
**大连工业大学 - 网络工程**
:::
:::right
**2020.09 - 2024.07**
:::

## 实习经验

:::left
**北京博华信智科技有限公司 - 前端实习生**
:::
:::right
**2025.03 – 2025.09**
:::

- 负责BHWebClient的开发与维护。

## 项目

### BHWebClient（工业设备智能监测平台）

`Vue.js` `Vuex` `Echarts` `Webpack` `Vue I18n`

- **项目描述**：面向工业设备（旋转机械、往复机泵等）的智能监测平台，提供振动波形、频谱、包络等多类图谱的实时展示与分析能力，支持中/英/俄三语切换与深浅主题，服务于设备预测性维护场景。
- **项目亮点**：
  - **多图表功能联动：** 单个监测页面常需同时展示趋势、波形、频谱等多张图谱，分析时需要跨图交互。将各类图谱封装为独立组件，通过事件总线实现趋势图选点驱动波形频谱重绘、刷选范围跨图同步、游标跨图共享。
  - **基于 ECharts graphic 的标注系统：** ECharts 原生标注不能拖拽，也无法自定义样式。基于底层 graphic 组件实现了可拖拽的数据标注，支持引线跟随、边界约束、右键编辑文字、自适应后重绘，用 `convertToPixel` 建立数据坐标和像素坐标的映射。
  - **图表渲染控制：** 浏览器窗口缩放时，图表容器大小会变化，需要重新计算图表尺寸。监听 `ResizeObserver` 事件触发 `echarts.resize` 重新绘制，结合防抖只在窗口变化结束后计算一次，避免频繁 resize 导致卡顿。
  - **定时任务管理：** 实时监测页面每隔几秒请求一次接口，切换页面后定时器仍在跑，浪费资源。结合 `keep-alive` 生命周期在页面激活时开启轮询、离开时清除定时器，避免无效请求。
  - **图表组件复用：** 各监测模块有大量相似图谱。抽取趋势图、波形图、频谱图、瀑布图等 60+ 公共图表组件。

### Sky Chat — AI 智能对话平台

`Next.js` `TypeScript` `Tailwind CSS` `Zustand` `Prisma` `PostgreSQL` `ECharts`

- **项目描述**：基于 Next.js 的 AI 聊天应用，支持与流式对话、图片生成、文件上传、会话管理，对话分享等功能，集成了 SSE 性能监测模块与 ECharts 可视化后台。
- **项目亮点**：
  - **基于 requestAnimationFrame 的渲染节流：** 流式对话中文本 delta 高频涌入直接驱动 UI 更新会造成渲染浪费。设计 `StreamBuffer` 层按 `requestAnimationFrame` 周期批量合并文本后统一更新（\~120fps → \~40fps），在保证实时性的同时解决渲染性能问题。
  - **流式 Markdown 渲染与未闭合标记处理：** 流式输出时 Markdown 标记常处于未闭合状态。实现 `closeOpenMarkdownBlocks` 函数实时检测代码块、行内代码、粗体等标记配对状态并自动补全，确保流式渲染不出现格式错乱。
  - **自研 SSE 性能监测模块与 ECharts 可视化看板：** 实现 `SSEPerformanceTracker` 采集 TTFB、TTLB、卡顿次数与时长等指标，IndexedDB 持久化后批量上报。管理后台基于 ECharts 搭建折线图、柱状图、散点图等可视化看板，量化分析流式传输性能。
  - **基于虚拟滚动的长对话列表：** 使用 `@tanstack/react-virtual` 实现虚拟滚动，动态估算行高，仅渲染可视区域消息，配合智能滚底控制，确保长对话流畅滚动。

## 技能

- **语言**：HTML5、CSS3、JavaScript、TypeScript
- **框架与库**：React、Next.js、Vue.js（Vuex/Pinia/Vue Router）、Zustand、ECharts
- **样式与 UI**：Tailwind CSS、Element Plus、shadcn/ui
- **工程化**：Webpack、Vite、Git
- **通信**：Axios、WebSocket、SSE

