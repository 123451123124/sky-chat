# Sky Chat 项目面试题

> 基于简历中四个项目亮点深挖，模拟面试场景。每道题从基础概念讲起，再深入到项目中的具体实现。

---

## 一、StreamBuffer — requestAnimationFrame 渲染节流

### 前置概念

**什么是 requestAnimationFrame（raf）？**

浏览器刷新屏幕的频率通常是 60 次/秒（60fps），每次刷新前浏览器会执行一个"渲染帧"。raf 是浏览器提供的 API，你传给 raf 的回调函数会在下一次渲染帧之前执行。适合做和渲染相关的操作。

**什么是 setTimeout？**

在指定延迟后执行回调，但**不保证和渲染帧同步**——可能在两次渲染帧之间执行多次，也可能跳过某次渲染帧。

**为什么流式对话中需要节流？**

SSE 流式传输中，服务器每秒可能推送几十个 text-delta，每个 delta 只有几个字。如果每个 delta 都触发一次 React setState，就会产生大量不必要的渲染（~120 次/秒），而人眼能感知的流畅变化只需要 ~30-40 次/秒。

---

### Q1：StreamBuffer 怎么实现渲染节流？

**答案：**

把高频涌入的文本 delta 先放进队列，然后通过 requestAnimationFrame 定期**批量取出合并**，一次性提交给 React 更新 UI。

核心流程：

```
SSE 推送 text-delta → push(delta) 入队列
                           │
                    有 raf 在排队吗？
                    ├─ 没有 → 发起一次 raf 请求
                    └─ 有 → 等待，什么都不做
                           │
                    浏览器渲染帧前 → raf 回调触发
                           │
                    flush() → 队列中所有数据取出合并成一条
                           → 一次性调用 appendTextDelta
                           → React 只渲染这一次
```

**关键设计：**

- raf 回调执行完后，如果队列中又有新数据，会再次发起 raf，形成连续消费链
- 数据涌入越快，raf 链越长；数据慢了，raf 链自然终止——**自动适配负载**
- `forceFlush()` 跳过 raf，立即消费队列，用于流结束时确保数据不残留

**为什么用 raf 而不是 setTimeout：**

| | raf | setTimeout |
|--|-----|-----------|
| 触发时机 | 每次渲染帧前，和浏览器同步 | 指定时间后，和渲染帧无关 |
| 一帧内触发次数 | 最多 1 次 | 可能多次 |
| 页面不可见时 | 自动暂停（不浪费） | 仍然触发（浪费） |
| 适合场景 | 和渲染相关的操作 | 通用延迟任务 |

---

### Q2：forceFlush 解决了什么问题？

**答案：**

流结束时，最后几个 delta 可能还在队列中等待下一次 raf 触发。如果流结束了不再有新的 delta 入队，raf 链就会终止，这些数据就永远渲染不出来了。

`forceFlush` 立即取消排队的 raf，直接把剩余数据全部消费掉，保证**"流结束 = 所有数据已显示"**。

```typescript
// ChatContainer 的 onFinish 回调中
onFinish: async () => {
  buffer.forceFlush();  // ← 强制清空队列，确保最后一段文本不丢失
  finalizeCurrentMessage();
  setStatus('idle');
}
```

---

### Q3：为什么客户端做缓冲，而不是让服务器降频推送？

**答案：**

服务器降频意味着"攒够一定字数再发"，这会增加用户的等待时间——用户要等更久才能看到第一个字（TTLB 增加）。

客户端缓冲是**自适应的**：

- CPU 空闲时 → 队列快速清空 → raf 链终止 → 几乎零延迟
- CPU 繁忙时 → 队列积压 → raf 自动节流 → 保护主线程

一句话：**服务器降频增加了所有场景的延迟，客户端缓冲只在需要时才节流。**

---

## 二、closeOpenMarkdownBlocks — 流式 Markdown 未闭合标记处理

### 前置概念

**什么是 chunk？**

SSE 流式传输中，服务器不会一次性发送整段文本，而是分多次推送。每次推送的一个数据片段就叫一个 chunk。

例如模型生成的 `"你好世界"` 可能被分成：

```
chunk 1: "你好"
chunk 2: "世界"
```

每个 chunk 就是 SSE 事件里的 `text-delta` 字段。

---

### Q4：closeOpenMarkdownBlocks 的原理和局限？

**答案：**

**原理：** 流式渲染时，Markdown 标记可能处于未闭合状态。函数统计三种标记（\`\`\`、**、\`）的出现次数，奇数就补上闭合标记。

```typescript
function closeOpenMarkdownBlocks(text: string): string {
  // 检测 ``` 出现次数，奇数则补上
  if (匹配到奇数个 ```) text += '\n```';
  // 检测 ** 出现次数，奇数则补上
  if (匹配到奇数个 **) text += '**';
  // 检测 ` 出现次数，奇数则补上
  if (匹配到奇数个 `) text += '`';
  return text;
}
```

**局限：三个标记独立检测，不考虑嵌套。**

例如代码块里的 `**` 是字面量，不是 Markdown 语法。但当前代码在代码块未闭合时，看到 `**` 出现奇数次仍然会追加 `**`，实际上是多余的。

**改进方案是用栈追踪：** 遇到 ` ``` ` 入栈，遇到闭合 ` ``` ` 出栈。只在栈为空（不在代码块内）时才去检测其他标记。

---

### Q5：还有哪些流式 Markdown 的难题？

**答案：**

比未闭合标记更难解决的问题是**语法被拦腰切断**。

例如链接 `[文档](https://x.com)` 被切成：

```
chunk 1: "请查看[文档"
chunk 2: "](https://x.com)查看"
```

- chunk 1 的 `"请查看[文档"` 不是一个合法链接，react-markdown 渲染为纯文本
- chunk 2 的 `"](https://x.com)查看"` 同样渲染为纯文本

最终用户看到的是纯文本 `[文档](https://x.com)`，链接不可点击。

**这个问题补闭合标记解决不了**——因为不是"缺了闭合符号"，而是"语法在中间被切断了"。

**两种处理思路：**

| 方案 | 做法 | 优点 | 缺点 |
|------|------|------|------|
| 增量渲染（本项目） | 每个 chunk 独立渲染 | 性能好 | 语法可能被切断 |
| 全量重渲染（Vercel AI SDK） | 保留完整文本，每次重新解析 | 语法永远完整 | 消息越长，重渲染越慢 |

---

## 三、SSE 性能监测与 ECharts 可视化

### 前置概念

**什么是 performance.now()？**

浏览器提供的高精度计时 API，从页面打开时开始计数，单位毫秒（精确到微秒）。和 `Date.now()` 的区别：

| | `performance.now()` | `Date.now()` |
|--|-------------------|-------------|
| 精度 | 微秒级 | 毫秒级 |
| 零点 | 页面打开时 | 1970 年 |
| 受系统时间影响 | 否（单调递增） | 是（用户改时间会变化） |

测量时间间隔（如 TTFB）必须用 `performance.now()`——如果用 `Date.now()`，用户修改系统时间会导致计算结果为负数。

**什么是 IndexedDB？**

浏览器提供的本地数据库，用于存储结构化数据。和 localStorage 的区别：

- 存储量大（通常 50MB+，localStorage 只有 5-10MB）
- 支持索引、事务、按主键查询
- 异步操作（localStorage 是同步的）

**什么是 sendBeacon？**

`navigator.sendBeacon(url, data)` 是浏览器专门用于"页面关闭时上报数据"的 API。它只支持 POST，不关心响应，**即使页面关闭了也能保证请求发出**。而普通的 fetch 在页面关闭时请求会被取消。

---

### Q6：SSEPerformanceTracker 怎么采集性能数据？

**答案：**

在流式请求的几个关键时机埋下 `performance.now()` 时间戳，流结束时汇总算出各项指标。

**埋点位置（在 ChatContainer 的流式回调中插入）：**

```typescript
// 创建 tracker
const tracker = new SSEPerformanceTracker({ sessionId });
tracker.start();  // ← 记录 startTime

// 流式回调中埋点
const callbacks = {
  onTextDelta: (_id, delta) => {
    buffer.push(delta);
    tracker.onChunk();  // ← 每次收到文本片段时调用
  },
  onTextStart: () => {
    tracker.onPhaseChange('answering');  // ← 进入文本生成阶段
  },
  onToolInputStart: () => {
    tracker.onPhaseChange('tool_calling'); // ← 进入工具调用阶段
  },
  onFinish: async () => {
    tracker.finish();  // ← 流结束时汇总计算
  },
};
```

**四个核心指标：**

| 指标 | 触发位置 | 怎么算 | 反映的问题 |
|------|---------|--------|-----------|
| TTFB | `onChunk()` 首次调用 | `首 chunk 时间 - startTime` | 模型首 token + 网络延迟 |
| TTLB | `finish()` | `结束时间 - startTime` | 完整回复等了多久 |
| Stall | `onChunk()` 每次调用 | 相邻两次调用间隔 >500ms 时累加 | 传输是否流畅 |
| Phase Duration | `onPhaseChange()` | 阶段切换时结算上一阶段耗时 | 哪个阶段最慢 |

**采集的数据存哪里？**

不直接发请求，先存 IndexedDB：

```typescript
// tracker.finish() 中
async finish() {
  // 计算指标 ...
  // 写入 IndexedDB（纯本地存储，不发起网络请求）
  await addToQueue({ type: 'ttfb', value: ttfb, ... });
  await addToQueue({ type: 'ttlb', value: ttlb, ... });
}
```

---

### Q7：怎么把数据上报到服务端？

**答案：**

采用"IndexedDB 暂存 + 定时批量发送"的模式。

```
采集 → IndexedDB 暂存
                 │
           每 10s 定时器 → 从 IndexedDB 读取所有数据
                 │        → 取前 20 条打包成一个请求
                 │        → POST /api/monitor
                 │        → 成功 → 删除已上报数据
                 │        → 失败 → 留到下次重试
                 │
           页面隐藏/关闭 → 立即触发一次上报（兜底）
```

**为什么这样设计：**

- **不逐条发送**：每产生一条指标就发一个请求，请求太频繁，服务端压力大
- **用 IndexedDB**：浏览器崩溃或网络闪断时数据不丢
- **页面关闭兜底**：visibilitychange + beforeunload + sendBeacon 三重保障

**代码实现：**

```typescript
// 定时器：每 10s 执行一次
setInterval(flushQueue, 10000);

async function flushQueue() {
  // 从 IndexedDB 读数据
  const queue = await getQueue();
  if (queue.length === 0) return;  // 没数据直接返回

  // 取前 20 条，打包成一个请求
  const batch = queue.slice(0, 20);
  await sendReport({ metrics: batch, url, userAgent, timestamp });

  // 发送成功，删除已上报的数据
  await clearQueue(batch.map(m => m.id));
}
```

---

### Q8：ECharts 渲染大量数据时怎么优化？

**答案：**

主要是**减少数据量**和**避免重复计算**：

1. **数据裁剪**：只取最新 100 条渲染，不随数据库增长而增加渲染量
2. **useMemo 缓存**：ECharts 的 option 对象用 `useMemo` 包裹，只有数据变化时才重建，避免每次组件重渲染都触发 ECharts reflow
3. **动态加载**：`next/dynamic` 只在客户端加载 ECharts，不参与 SSR

如果数据量到数十万条，再加三层：

- **服务端聚合**：按小时取平均值/P95，返回少量数据点
- **前端采样**：等距取点，保留趋势特征
- **ECharts large 模式**：开启 Canvas 2D 批量渲染，绕过 SVG 的点数限制

---

## 四、虚拟滚动与长对话列表

### 前置概念

**什么是虚拟滚动？**

列表很长时（比如 100 条消息），不创建所有消息的 DOM，只渲染用户当前能看到的那几条。不可见的消息用空白占位，保证滚动条表现正确。

```
┌───────────────────┐
│ 消息 1  (不可见)   │  ← 不建 DOM，占位
│ 消息 2  (不可见)   │
├───────────────────┤
│ 消息 3  (可见)     │  ← 只渲染这 3 条
│ 消息 4  (可见)     │
│ 消息 5  (可见)     │
├───────────────────┤
│ 消息 6  (不可见)   │  ← 不建 DOM，占位
│ ...               │
│ 消息 100 (不可见)  │
└───────────────────┘
```

**为什么需要虚拟滚动？**

100 条普通消息 ≈ 200+ DOM 节点，滚动流畅。但聊天消息可能包含代码块、图片、表格，DOM 节点数会暴增。虚拟滚动把实时 DOM 节点数控制在 10-15 个，不管列表多长。

---

### Q9：项目中的虚拟滚动怎么实现的？

**答案：**

使用 `@tanstack/react-virtual`，核心是三步：

**第一步：创建虚拟滚动器**

```typescript
import { useVirtualizer } from "@tanstack/react-virtual";

const virtualizer = useVirtualizer({
  count: messages.length,                    // 总消息数
  getScrollElement: () => parentRef.current,  // 滚动容器 DOM
  estimateSize: (index) => {                  // 每项高度估算
    if (消息短) return 80;
    if (消息中等) return 120;
    if (消息长) return 280;
  },
  overscan: 5,  // 可视区上下多渲染 5 条，快速滚动不白屏
});
```

**第二步：三层容器结构**

```html
<!-- 外层：固定高度，可滚动 -->
<div ref={parentRef} style={{ height: '100%', overflowY: 'auto' }}>

  <!-- 中层：总高度占位，让滚动条和真实列表一致 -->
  <div style={{ height: virtualizer.getTotalSize(), position: 'relative' }}>

    <!-- 内层：只渲染可见的消息 -->
    {virtualizer.getVirtualItems().map(item => (
      <div
        key={item.key}
        data-index={item.index}
        ref={virtualizer.measureElement}  <!-- 渲染后自动测量真实高度 -->
        style={{
          position: 'absolute',
          transform: `translateY(${item.start}px)`,  <!-- 定位 -->
        }}
      >
        <MessageBubble message={messages[item.index]} />
      </div>
    ))}

  </div>
</div>
```

**第三步：智能滚底控制**

新消息到来时，如果用户在底部就自动滚底，在看历史就不打断：

```typescript
// 监听滚动位置
const handleScroll = () => {
  const atBottom = 距离底部 < 150px;
  isUserScrolledUp.current = !atBottom;  // 不在底部 → 用户在看历史
};

// 新消息到来时
useEffect(() => {
  if (!isUserScrolledUp.current) {
    scrollToBottom();  // 用户在底部 → 自动滚底
  }
}, [messages]);
```

**几个关键细节：**

- `getScrollElement: () => parentRef.current` 用函数形式而非直接传值，因为初始化时 ref 还是 null，函数在库内部需要时才调用
- `measureElement` 绑在 translateY 定位的 div 上，测量结果 = 消息在列表中占用的真实空间
- `data-index={item.index}` 告诉库"这个 DOM 对应第几个虚拟项"
- 如果不绑 `measureElement`，行高永远用估算值，长列表滚动到底部时位置会偏

---

### Q10：智能滚底有什么缺陷？怎么改进？

**答案：**

当前逻辑：用户向上滚动超过 150px 就认为"在看历史"，不再自动滚底。

**缺陷 1：位置不能代表意图**

用户可能在底部但正在阅读最后几行，新消息到来时系统误判为"在底部"并滚底，打断阅读。

**改进：追踪滚动方向而非位置**

```typescript
// 用户主动向上滚动 → 锁定不滚底
// 用户主动滚回底部 → 解锁
const handleScroll = () => {
  const isUp = scrollTop < lastScrollTop;
  if (isUp) hasScrolledUp = true;
  if (到底了 && hasScrolledUp) hasScrolledUp = false;
};
```

**缺陷 2：新消息推离视口（Scroll Anchoring）**

用户在看历史消息时，新消息使 DOM 增长，当前阅读的内容被往上推走。

**改进：补偿滚动**

检测到用户在看历史且 DOM 高度增加时，自动增加 scrollTop，抵消内容上推，保持阅读位置稳定。

---

## 五、综合架构

### Q11：完整的数据流是怎样的？

**答案：**

```
用户发送消息
  → handleSend(text)
    → 显示用户消息
    → setStatus('thinking')
    → streamRequest()
      → tracker.start()
      → fetch POST /api/chat (SSE 流)
        → 服务端：
          验证 JWT → 调 OpenAI API → 解析流 → 输出自定义 SSE
          支持最多 5 步循环（生成文本 → 调工具 → 再生成）
        → 客户端读取流：
          reader.read() → TextDecoder → SSEParser 切分事件
          → parseAIStreamChunk JSON 解析
          → handleStreamChunk 分发
            onTextDelta → StreamBuffer 缓冲 → raf → appendTextDelta
            onToolInputStart → 显示工具卡片
            onToolOutputAvailable → 显示工具结果
            onFinish → forceFlush + 保存消息 + tracker.finish()
            onError → 显示错误
      → 清理 buffer 和 AbortController
```

**错误处理：**

| 错误 | 表现 |
|------|------|
| 网络异常 | "网络错误，请重试" |
| 用户停止 | 静默（不报错） |
| API 错误 | "API error: 详情" |
| 工具失败 | "图片生成服务异常" |
| 单个 chunk 解析失败 | 静默跳过 |

---

### Q12：为什么自定义 SSE 协议而不是用 AI SDK？

**答案：**

项目需要三个 Vercel AI SDK 的 `streamText` 无法直接满足的能力：

1. **多步工具调用的手动控制**：需要循环"生成→调工具→放回结果→再生成"最多 5 轮，`streamText` 的 maxToolCalls 不支持这种精细控制
2. **14 种 chunk 类型**：text-start/delta/end、reasoning、tool-input/output、step-start、finish、error、abort，每种对应不同的 UI 状态变化
3. **客户端状态机**：thinking → tool_calling → answering → idle 完全由客户端根据 chunk 类型控制

**代价：** 需要自建 SSE 解析器、流缓冲、状态机，协议不兼容标准格式。

---

### Q13：为什么用 Zustand 而不是 Context 或 Redux？

**答案：**

- **vs Context**：流式场景每秒数十次更新，Context 会让所有 consumer 重渲染。Zustand 支持精确订阅——`MessageList` 只订阅 `messages`，`ThinkingIndicator` 只订阅 `status`，互不干扰
- **vs Redux**：聊天状态机就 ~150 行逻辑，Redux 的 action/reducer 分离引入过多样板代码

```typescript
// Zustand：精确订阅 + 零 Provider
const status = useChatStore(s => s.status);      // 只订阅 status
const messages = useChatStore(s => s.messages);   // 只订阅 messages
```

---

### Q14：多租户要改什么？

**答案：**

当前 API Key 在环境变量里，所有用户用同一个。多租户需要：

- **数据层**：新增 Tenant 表存 API Key（加密）、Base URL、model；User 关联 Tenant
- **认证层**：JWT 携带 tenantId
- **路由层**：/api/chat 动态加载租户配置，不从环境变量读
- **安全层**：API Key AES-256 加密入库，运行时解密

---

### Q15：状态机的合法转换和防御？

**答案：**

```
idle → thinking        发送消息
thinking → answering   收到文本
thinking → tool_calling 收到工具调用
tool_calling → answering 工具执行完毕
answering → idle        流结束
任意 → error            异常
任意 → idle             用户停止
```

防御：按钮禁用、每次请求前 abort 上一次、入口处双重检查 status。

---

### Q16：SSEParser 怎么处理 TCP 分包？

**答案：**

维护一个内部 buffer。每次收到新数据时拼接到 buffer 末尾，查找 `\n\n`（SSE 事件分隔符），找到就切出来解析，没找到就等下次。

```
收到 "data: hello\n\ndata: wo"
  → 找到 \n\n，切出 "data: hello"，buffer 剩 "data: wo"
收到 "rld\n\n"
  → buffer = "data: world\n\n"，切出 "data: world"
```

--- 

### Q17：项目值得改进的地方？

**答案：**

1. **文件读取安全**：`file.text()` 整体读入再截断，大文件会导致 OOM。改成 `file.slice(0, 50000).text()`
2. **缺少超时**：30 秒没响应用户只能手动停止。加三层超时——连接超时 30s、首字节超时 15s、buffer 空转超时
3. **消息没缓存**：每次刷新从 API 重载。IndexedDB 做消息缓存可弱网访问
4. **固定 500ms 阈值**：不同网络环境不准确。用滑动窗口 P95 做动态阈值

---

### Q18：长卡顿怎么定位原因？

**答案：**

| 现象 | 可能原因 |
|------|---------|
| TTFB 高 | 模型推理慢 / 网络慢 |
| TTLB - TTFB 差值大 | 流中持续抖动 |
| tool_calling 阶段卡 | 工具 API 慢 |
| 连续高 stall | 服务器 GC / 网络波动 |

通过后台的 TTFB/TTLB/Stall 关联表格排查。改进：加全链路 trace ID，三级追踪。

---

### Q19：重新设计会改什么？

**答案：**

1. SSE 协议标准化 → 复用社区工具
2. IndexedDB 消息缓存 → 弱网可用
3. 动态 stall 阈值 → 自适应网络
4. 加超时 → 防止卡死
5. React.memo 隔离 → 减少不必要渲染

---

> **面试官视角**
>
> 回答时说清楚三件事：**为什么选这个方案、极端情况会怎样、你知道哪里可以做得更好。** 展示的是技术决策能力和工程思维成熟度。
